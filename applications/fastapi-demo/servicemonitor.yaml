apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor

metadata:
  name: fastapi-demo
  namespace: observability
  labels:
    release: monitoring

spec:
  selector:
    matchLabels:
      app: fastapi-demo

  endpoints:
  - port: http
    path: /metrics
    interval: 15s