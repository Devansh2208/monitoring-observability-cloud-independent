apiVersion: v1
kind: Service

metadata:
  name: fastapi-demo
  namespace: observability
  labels:
    app: fastapi-demo

spec:
  selector:
    app: fastapi-demo

  ports:
  - name: http
    port: 8000
    targetPort: 8000